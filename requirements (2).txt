# ============================================================
# Predicción de Migración Interna — GEIH 2025
# Dependencias del proyecto (todas las librerías utilizadas)
# ============================================================

# --- Manipulación de datos ---
numpy==2.4.4
pandas==3.0.2

# --- Machine learning ---
scikit-learn==1.8.0
xgboost==3.2.0

# --- Balanceo de clases (ImbPipeline, TomekLinks, ClusterCentroids) ---
imbalanced-learn==0.14.1

# --- Clustering con variables mixtas: numéricas + categóricas (KPrototypes) ---
kmodes==0.12.2

# --- Interpretabilidad (SHAP: TreeExplainer, LinearExplainer, summary_plot) ---
shap==0.51.0

# --- Visualización ---
matplotlib==3.10.8
seaborn==0.13.2

# --- Datos geoespaciales (mapas departamentales con shapefile GADM) ---
geopandas==1.1.3

# --- Dependencias de soporte (usadas internamente por las librerías anteriores) ---
scipy==1.17.1
joblib==1.5.3

# --- Entorno de ejecución ---
# google-colab  # Preinstalado en Colab; necesario solo para drive.mount()
#               # En entorno local no se requiere.
